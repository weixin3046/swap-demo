export default function ImportWallet() {
  const addAccount = () => {
    // const baseWalletNew = baseWallet.derivePath(i.toString());
  };
  return (
    <div>
      <button>添加账号</button>
    </div>
  );
}
